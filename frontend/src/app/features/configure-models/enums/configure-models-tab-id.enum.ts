export enum ConfigureModelsTabId {
    QUICKSTART = 'quickstart',
    DEFAULT_LLMS = 'default_llms',
    LLM_LIBRARY = 'llm_library',
    WEBHOOK_TRIGGERS = 'webhook_triggers',
    VOICE_SETTINGS = 'voice_settings',
}
