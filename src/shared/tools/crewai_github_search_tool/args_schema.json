{
  "title": "LocalRemoteGithubSearchToolArgs",
  "type": "object",
  "properties": {
    "search_query": {
      "type": "string",
      "description": "Mandatory search query to find in the local or remote GitHub repo"
    },
    "repo_path_or_url": {
      "type": "string",
      "description": "Path to the local cloned repo or GitHub URL of the repo"
    },
    "file_types": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional file types to include in search, e.g., ['.py', '.md']"
    }
  },
  "required": ["search_query", "repo_path_or_url"]
}