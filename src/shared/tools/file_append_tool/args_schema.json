{
  "title": "AppendFileArgs",
  "type": "object",
  "properties": {
    "file_path": {
      "type": "string",
      "description": "Mandatory file full path to append the text."
    },
    "append_text": {
      "type": "string",
      "description": "Mandatory text to be appended to the file."
    }
  },
  "required": ["file_path", "append_text"]
}
